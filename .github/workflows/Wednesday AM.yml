name: Wednesday AM

on:
  workflow_dispatch:
  schedule:
    - cron: "1 0 * * 3"

jobs:
  Push: 
   runs-on: ubuntu-latest
   steps:
    - name: what's the time
      run: date
    - name: webgl
      run: |
          echo "what are you doing"