name: Monday PM

on:
  workflow_dispatch:
  schedule:
    - cron: "1 10 * * 1"

jobs:
  Back: 
   runs-on: ubuntu-latest
   steps:
    - name: what's the time
      run: date
    - name: WebGL2 Fundamentals
      run: echo "how it's going"
    - name: 2D translation, rotation, scale, matrix math
      run: |
          echo "WebGL2 2D Translation"
          echo "WebGL2 2D Rotation"
          echo "dot product ? cross product?"
          echo "WebGL2 2D Scale"
          echo "WebGL2 2D Matrices"