name: Friday PM

on:
  workflow_dispatch:
  schedule:
    - cron: "1 10 * * 5"

jobs:
  Back: 
   runs-on: ubuntu-latest
   steps:
    - name: what's the time
      run: date
    - run: |
        echo "WebGL2 2D Rotation"
        sleep 5
        echo "x1*y2+y1*x2"
        sleep 5
        echo "y1*y2-x1*x2"
        sleep 5
        echo "help monday"