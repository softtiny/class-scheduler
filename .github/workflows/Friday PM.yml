name: Friday PM

on:
  workflow_dispatch:
  schedule:
    - cron: "1 10 * * 5"

jobs:
  Back: 
   runs-on: ubuntu-latest
   steps:
    - name: what's the time
      run: date
    - run: |
        echo "lag lag lag"
        sleep 5
        echo "ip port ok...."
        sleep 5
        echo "connect ip port"
        sleep 5
        echo "remote test ?"