name: Friday PM

on:
  workflow_dispatch:
  schedule:
    - cron: "1 10 * * 5"

jobs:
  Back: 
   runs-on: ubuntu-latest
   steps:
    - name: what's the time
      run: date
    - run: |
        echo "what's up"
        sleep 5
        echo "what's your job"
        sleep 5
        echo "Jobs you might like "
        sleep 5
        echo "Invitations to interview ?"