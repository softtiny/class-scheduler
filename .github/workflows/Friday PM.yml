name: Friday PM

on:
  workflow_dispatch:
  schedule:
    - cron: "1 10 * * 5"

jobs:
  Back: 
   runs-on: ubuntu-latest
   steps:
    - name: what's the time
      run: date
    - run: |
        echo "kali down"
        sleep 5
        echo "remote gone"
        sleep 5
        echo "next Monday"
        sleep 5
        echo "help monday"