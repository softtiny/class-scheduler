name: Tuesday PM

on:
  workflow_dispatch:
  schedule:
    - cron: "1 10 * * 2"

jobs:
  Back: 
   runs-on: ubuntu-latest
   steps:
    - name: what's the time
      run: date
    - name: WebGL2 Image Processing
      run: |
          echo "textures draw image"