name: Tuesday PM

on:
  workflow_dispatch:
  schedule:
    - cron: "1 10 * * 2"

jobs:
  Back: 
   runs-on: ubuntu-latest
   steps:
    - name: what's the time
      run: date
    - name: WebGL2 3D Perspective
      run: |
          echo "set up the w-coordinate so that dividing by it performs a perspective divide."