name: Wednesday PM

on:
  workflow_dispatch:
  schedule:
    - cron: "1 10 * * 3"

jobs:
  Back: 
   runs-on: ubuntu-latest
   steps:
    - name: what's the time
      run: date
    - name: git patch
      run: echo "git format-patch ?"
    - name: git stash remote
      run: echo "git stash import/export" 