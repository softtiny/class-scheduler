name: Wednesday PM

on:
  workflow_dispatch:
  schedule:
    - cron: "1 10 * * 3"

jobs:
  Back: 
   runs-on: ubuntu-latest
   steps:
    - name: what's the time
      run: date
    - name: image program
      run: echo "texture uniform rgb"