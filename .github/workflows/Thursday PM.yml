name: Thursday PM

on:
  workflow_dispatch:
  schedule:
    - cron: "1 10 * * 4"

jobs:
  Back: 
   runs-on: ubuntu-latest
   steps:
    - name: what's the time
      run: date
    - name: WebGL2 2D Rotation
      run: echo " 123 * 1 = 123"
    - name: a unit circle? what
      run: echo "multiply geometry"