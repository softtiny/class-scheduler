name: Thursday PM

on:
  workflow_dispatch:
  schedule:
    - cron: "1 10 * * 4"

jobs:
  Back: 
   runs-on: ubuntu-latest
   steps:
    - name: what's the time
      run: date
    - name: WebGL2 3D Perspective
      run: echo "mat4.perspective(out, fov, aspect, near, far)"