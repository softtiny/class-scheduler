name: Thursday PM

on:
  workflow_dispatch:
  schedule:
    - cron: "1 10 * * 4"

jobs:
  Back: 
   runs-on: ubuntu-latest
   steps:
    - name: what's the time
      run: date
    - name: Further Pure Mathematics
      run: echo "Matrices and transformations"